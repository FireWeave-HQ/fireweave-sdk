"""application/ — runtime engine, client facade, and the mode.py composition
root. Depends on `domain/` only, except `mode.py` (the sanctioned composition
root, which selects and constructs concrete `infrastructure/adapters/*`).
"""
